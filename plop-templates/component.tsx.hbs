import { Box, Paper, Text } from '@mantine/core';
import { useTranslation } from 'react-i18next';

import classes from './{{pascalCase name}}.module.css';

export type {{pascalCase name}}Props = {
  // adicione suas props aqui
};

export function {{pascalCase name}}({}: {{pascalCase name}}Props) {
  const { t } = useTranslation();

  return (
    <Paper withBorder p="md" className={classes.wrapper}>
      <Box>
        <Text>
          {t('{{camelCase name}}.welcome', { ns: '{{camelCase name}}' })}
        </Text>
        <Text c="dimmed" size="sm">
          {t('{{camelCase name}}.description', { ns: '{{camelCase name}}' })}
        </Text>
      </Box>
    </Paper>
  );
} 